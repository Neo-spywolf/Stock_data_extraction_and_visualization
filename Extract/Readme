Make sure you have the necessary libraries (matplotlib, pandas, requests, BeautifulSoup) imported before running this code. Additionally, ensure that your data sources are accessible and correctly formatted for the code to work as expected.
pip install yfinance
pip install requests
pip install beautifulsoup4
pip install pandas
